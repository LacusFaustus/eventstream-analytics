apiVersion: v1
kind: Service
metadata:
  name: analytics-api
  namespace: analytics
spec:
  selector:
    app: analytics-api
  ports:
    - port: 8088
      targetPort: 8088
      protocol: TCP
  type: LoadBalancer